services:
  app:
    build: .
    networks:
      - lb-net
  load-balancer:
    image: nginx:alpine 
    volumes:
      - ./nginx-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - lb-net
    depends_on:
      - app
    ports:
      - 8080:80
networks:
  lb-net: 
    driver: bridge  